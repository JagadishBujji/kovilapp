import { createContext } from "react";

const StoreContext = createContext({});

const StoreContextProvider = (props) => {
    const [ticket, setCurrentDevice] = useState();
};

export default StoreContext;