import { query } from "express";
import { collection, getDocs, where } from "firebase/firestore";
import { useEffect } from "react";
import { db } from "../../services/firebase";

 
 const EditPD = ({formData,setFormData}) => {
    
  return (
 <>
 </>
  )
}

export default EditPD